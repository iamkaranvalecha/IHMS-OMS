# Shared networks, volumes, and environment anchors for layered compose files.
# Note: YAML anchors do not cross compose -f files; define x-orchestrator-* in each layer.

networks:
  checkout-net:
    driver: bridge

volumes:
  catalog-data:
  prometheus-data:
