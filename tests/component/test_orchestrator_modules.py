"""Component tests — orchestrator modules together, no real upstreams."""


def test_component_scaffold_passes() -> None:
    """Placeholder until gateway + session + catalog are wired."""
    assert True
