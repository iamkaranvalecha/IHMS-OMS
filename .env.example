# Optional overrides — copy to .env for real upstreams (KB-IHMS docker + EC-OPS on host)
#
# IHMS_BASE_URL=http://host.docker.internal:5000
# ECOPS_BASE_URL=http://host.docker.internal:8002
# ECOPS_BEARER_TOKEN=
# UI_PORT=5180
