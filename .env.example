# Optional overrides — copy to .env

# Default stack uses host ports that avoid KB-IHMS (5173) and EC-OPS (8002):
#   UI 5180 | mock EC-OPS 8012 | orchestrator 8000 | mock IHMS 8080

# Real upstreams (KB-IHMS docker + EC-OPS on host) — then:
#   docker compose up orchestrator ui --no-deps --build
IHMS_BASE_URL=http://host.docker.internal:5000
ECOPS_BASE_URL=http://host.docker.internal:8002
ECOPS_BEARER_TOKEN=
